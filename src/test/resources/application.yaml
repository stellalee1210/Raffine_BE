spring:
  datasource:
    url: jdbc:h2:mem:raffine-test;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  upload-dir: ${java.io.tmpdir}/raffine-test-uploads